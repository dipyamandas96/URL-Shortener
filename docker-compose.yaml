services:
  shrinkly-server:
    build: ./server
    env_file:
      - ./server/.env
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped

  shrinkly-client: 
    build: ./client
    env_file:
      - ./client/.env
    ports:
      - 3000:3000